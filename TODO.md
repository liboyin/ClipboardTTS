# TODO — Future Work

Read [AGENTS.md](AGENTS.md), [USER_STORIES.md](USER_STORIES.md), and [README.md](README.md) before accepting an implementation task. This file owns outstanding work, user decisions, and task boundaries. AGENTS owns working policy; README owns current architecture and operating instructions; USER_STORIES owns product behavior.

This plan replaces the former phase sequence and phase-closure fences. Dependencies below govern execution. A recorded finding is not permission to combine unrelated changes or redesign the app. Accept a bounded assignment, re-read its code, and implement one independent change per commit. Expand a compact backlog entry into a full boundary before implementing it.

## Maintaining this plan

This section owns how TODO is updated. General working principles belong in AGENTS; the review skill owns the review procedure. Keep this file focused on decisions and work that still matter.

1. Give each finding a stable ID, severity, evidence status, relevant paths, expected outcome, validation criteria, and dependencies. Keep hypotheses distinguishable from validated defects; preserve reviewer classifications and record user dispositions. Do not recycle IDs.
2. Keep unassigned work compact. Before implementation, expand the assigned task into a full boundary: intent, dependencies, implementation direction, non-goals, validation, and done criteria. Link that boundary from the finding instead of duplicating it.
3. Record accepted decisions with their rationale and affected tasks. Keep unresolved choices explicit. When a decision changes, record its supersession and update affected boundaries and dependencies before the implementation commit.
4. Use explicit dependencies and readiness to guide execution. Do not rebuild phase-closure fences or imply that every non-blocking issue must land before unrelated work can proceed.
5. In each implementation commit, remove the completed task's full active entry and execution boundary. Update references, dependencies, legacy mappings, and any affected counts or indexes. Keep at most a concise disposition or governing-document/commit link needed for remaining work or traceability. Put implementation history in the commit and current operations in README.
6. Record deferral or acceptance without change explicitly, with the reason and any revisit trigger. Do not silently drop inherited work or treat a documentation rewrite as an implementation fix.
7. Keep evidence tied to its revision and provenance. Remove obsolete status inventories; historical review results do not certify an executor's current gates. Check local links and ID/dependency consistency after restructuring this file.

## Evidence provenance

The findings come from the 2026-09-06 full-project review of cf39ce5a9777a9acf150bab0a4b93dade6798d4e, using Xcode 26.6 (17F113) and SwiftLint 0.65.1. Commands and bounded scratch probes are recorded in that review conversation; scratch artifacts were removed. Probe descriptions below are reproduction recipes, not checked-in tests or reusable gate certificates. Revalidate affected claims and remedies against the execution revision.

The review did not exercise live providers, physical device switching, macOS 13, or Thread Sanitizer. Device-change evidence used an owned engine and a simulated notification. The inherited Task 42 whole-suite mutant result was not rerun. N5's completed disposition records the relevant toolchain warnings.

A follow-up sequential review on 2026-09-18 covered the Codex commits `afccaff`–`379cd97` and produced NB26–NB29 and N6. It was read-only against `ec01095`: it inspected the ten diffs, the affected source and tests, `project.yml`, the generated scheme list, and `swiftlint --strict` (0 violations in 95 files). It did not rerun the test suite, the coverage gate, or the mutation evidence those commits' dispositions claim.

N7 comes from NB12's gate run on 2026-09-18 at `6578dac`, which used Xcode 27.0 (27A266a) and Swift 6.4 rather than the Xcode 26.6 recorded above. Toolchain claims recorded against 26.6 have not been revalidated on 27.0.

**Validated** means source inspection and/or the stated review probe supported the finding. **Not independently validated** identifies inherited evidence still needing verification. Architectural benefits are design judgments, even when the underlying structure is validated.

## Decisions and constraints

### Accepted user decisions — 2026-09-06

| ID | Decision | Applies to |
|---|---|---|
| D1 | Introduce one speech-session coordinator incrementally before fixing underruns. It owns start/cancel, the selected audio format, and ordered completion. | NB19, B2, NB9 |
| D2 | Applying a changed live PCM format clears the buffer and cancels its paired network request. Do not defer the change until the next session. | NB9 |
| D3 | Discard a trailing partial PCM byte silently when complete frames exist. Empty and single-byte responses still fail. | NB2 |
| D4 | OpenAI-compatible endpoints may omit Content-Type or use a generic type; reject explicitly unsupported formats. Validate Gemini's native PCM declaration. | NB1 |
| D5 | Refuse cross-origin credential-bearing redirects. Preserve credentials deliberately only on permitted same-origin redirects, subject to the transport rule. | NB4 |
| D6 | Use an explicit ephemeral policy with no persistent cache, shared cookie store, or credential store. | NB5 |
| D7 | Expand the 85% aggregate coverage gate to application logic outside Managers, with narrow documented platform UI/integration exceptions. | NB16 |
| D8 | Remove the unused clipboard-presence console messages. | NB24 |
| D9 | Keep AGENTS concise and principle-based, the review skill procedural, and TODO focused on future work with its own maintenance rules. This separates stable working policy from review operations and evolving plans. | Documentation ownership |

### Accepted user decisions — 2026-09-17

| ID | Decision | Applies to |
|---|---|---|
| D10 | An audio configuration change pauses the session at its position: restart the engine on the new output, keep the buffer and the running request, and cancel any pending automatic start or underrun resume. Play resumes from that position. Speech never moves to a different output device unasked. | NB11 |
| D11 | A new speech attempt (menu, Services, Test Voice) retries starting a stopped engine instead of silently refusing. Success clears the stale failure and starts speech; failure keeps "Couldn't start audio playback. Try again." visible and starts no request. | NB11 |

### Accepted user decisions — 2026-09-18

| ID | Decision | Applies to |
|---|---|---|
| D12 | Raise `ClipboardTTSApp`'s deployment target to macOS 14, the version the gates actually exercise, rather than keep a macOS 13 claim evidenced only by manual smoke checks. macOS 13 support is dropped. | NB29 |
| D13 | Cap one Gemini SSE event at 64 MiB before it ends, and fail a stream that exceeds it on the existing no-playable-audio path. The largest documented event is about 42 MB, which leaves roughly 50% headroom. That figure comes from each Gemini TTS model's 16,384-token output limit at the 25 audio tokens per second Google bills, as 24-kHz 16-bit mono PCM, base64-encoded. | NB6 |
| D14 | A request-state publication that an observer triggers while another publication is still running takes effect as soon as the outermost one finishes, in order and in the same main-queue turn, instead of inside it or on a later main-queue turn. State is then correct when the outer call returns, and main-queue work queued earlier cannot see a stopped request still marked as streaming. | NB8 |

### Accepted user decisions — 2026-09-20

| ID | Decision | Applies to |
|---|---|---|
| D15 | Expanding the gate must not weaken the floor it already has. `Sources/Managers/` keeps its own 85% verdict beside the new 85% verdict over all gated application logic, because a single combined aggregate would let Managers fall to roughly 78% before the combined number dipped, absorbed by the high-coverage views. | NB16 |
| D16 | Exclusions name symbols, not files or directories. Moving production code so that an exclusion could cover a whole file is refused: the adapters share their files with real logic, and per-function coverage records make the narrower arithmetic exact. | NB16 |
| D17 | Each exclusion's end-to-end smoke check is written down and run by the user rather than by the agent, and TODO records until then that the agent has not performed it. | NB16 |

### Accepted user decisions — 2026-09-21

| ID | Decision | Applies to |
|---|---|---|
| D18 | NB20 lands as two independent extractions with a commit and gates each: the pure provider catalogs and the OpenAI-compatible discovery contract first, then the manager's resolved request configuration. | NB20 |
| D19 | The static catalogs keep being published through the guarded metadata token path. The provider identity each published list carries is what lets a Settings form refuse the previous provider's list on the render before `onChange` resynchronizes the manager, so moving that protection into the form is not part of NB20. | NB20 |
| D20 | NB21 is accepted without change. Both layers it names carry behavior with no cheaper owner at this baseline, and replacing the hosted control presses with command-level tests would need its own boundary. | NB21 |

These choices do not need to be asked again. Validate exact implementation details against the repository and version-appropriate APIs. Raise a newly discovered material trade-off before changing the boundary; do not reinterpret an accepted decision silently.

### Constraints that remain in force

- Preserve USER_STORIES: the fixed menu icon, Settings-only voice selection, two-click Clear Buffer/Speak behavior, and the clipboard-only OpenAI input-length refusal.
- Preserve README's immutable request snapshots, ordered generation-authorized callbacks, synchronous cancellation authority, and single bounded Gemini HTTP 500 retry. Completion cannot overtake accepted PCM; published isStreaming is not an end-of-stream signal.
- Preserve the 0.1-second initial prebuffer, 0.2-second deferred clipboard policy, exact-end seek/replay, and finite Custom sample rates from 8000 through 48000, including fractions.
- Tests state settings through storage they own (`makeOwnedDefaults`), never the developer's preferences. Do not reintroduce snapshot/clear/restore of the app's own domain, and do not seed real preferences to prove that tests avoid them; a scratch checkout alone does not isolate macOS preferences.

### Investigation gates

NB20's and NB21's inventories were taken on 2026-09-21, produced D18–D20, and are closed; NB25 must still close the provider-only half of its metadata test gap, whose survival NB20's configuration disposition reproduces. These investigations can expose new decisions; they do not authorize guessing compatibility limits or removing coverage.

## Active backlog

Finding IDs preserve the full-project review's numbering; NB26–NB29 and N6 continue that sequence from the 2026-09-18 commit review, and N7 from NB12's gate run, both recorded under Evidence provenance. NB22 is completed by this documentation change and appears under dispositions. NB25 preserves inherited Task 42. Severity and readiness are separate: needing a decision does not make a defect non-blocking.

### Blocking

No Blocking finding is outstanding; B2 was the last one and appears under dispositions.

### Non-blocking

#### NB17 — Test teardown does not own every queued audio action

**Validated — ownership trace.** Fixed waits remain in Services/audio/metadata tests. Draining a network delivery queue does not drain the player buffer queue, publications, or pending automatic start. This is a missing guarantee, not a claim the baseline observed an escaping callback. **Paths:** [Services tests](Tests/ServicesCoordinatorTests.swift), [audio tests](Tests/AudioPlayerManagerTests.swift), [test factory](Tests/TestNetworkSupport.swift), [audio owner](Sources/Managers/AudioPlayerManager.swift), metadata tests. **Direction/acceptance:** test-owned audio lifecycle, explicit processing/publication completion, cancelled scheduled work, unconditional teardown, and locally counted late work. Forced delayed callbacks cannot escape even after assertion timeouts. Preserve network scope accounting.

#### NB18 — Pure tests inherit heavyweight integration dependencies

**Validated — construction trace.** Secret/state tests inherit the global network lifecycle; an About test creates audio/network managers solely to forward metadata. **Paths:** [SecretStoreTests](Tests/SecretStoreTests.swift), [SettingsSecretStateTests](Tests/SettingsSecretStateTests.swift), [SettingsAboutTests](Tests/SettingsAboutTests.swift), test support. **Direction/acceptance:** test pure units with owned memory dependencies and no audio/session resources; retain a small hosted wiring suite. Meaningful mutants must still detect protected behavior. Improved preferences ownership alone does not justify removing shared asynchronous safeguards.

#### NB21 — Production layers accommodate windowless test-harness limitations

**Validated — architectural observation.** SettingsActionButton exposes AppKit controls to the host; About uses three protocols plus metadata/routing adapters for a small operation. **Paths:** [SettingsActionButton](Sources/Views/SettingsActionButton.swift), [AboutAction](Sources/AboutAction.swift), [HostedSettings](Tests/HostedSettings.swift), related tests. **Direction:** assess command-level tests plus a small wiring suite before removing wrappers. **Acceptance:** preserve appearance, actionable controls, accessibility, metadata fallbacks, license credits/link, and main-thread presentation. Give unique behavior a replacement owner first. A new UI-test target or changed interaction needs its own agreed boundary; do not delete coverage merely to simplify the production tree.

#### NB24 — Clipboard-presence console messages have no release purpose

**Validated — print calls; legacy Task 46.** They disclose clipboard presence, not copied text. **Paths:** [TextExtractionManager](Sources/Managers/TextExtractionManager.swift), [text tests](Tests/TextExtractionManagerTests.swift), README if a diagnostic description changes. **Direction/acceptance:** implement D8 by removing the prints. Preserve populated/empty injected clipboard returns; introduce no new error or alternative logging; never use the shared pasteboard.

#### NB25 — Provider-only metadata invalidation needs current falsification evidence

**Validated — reproduced 2026-09-21 at `c3ed76a` plus the landed NB20 configuration change.** The check still compares endpoint and provider. OpenAI and Custom can share a default endpoint, so provider-only invalidation remains meaningful, and a mutant dropping the provider from that comparison survives the whole suite, where the endpoint half and a needless-invalidation over-restriction each fail a named test (see NB20's configuration disposition). The inherited Task 42 claim is therefore current rather than historical, and what remains is the provider-only half. **Paths:** [updateSettings](Sources/Managers/TTSNetworkManager.swift), [metadata source tests](Tests/TTSNetworkManagerMetadataSourceTests.swift), README. **Direction/acceptance:** verify pending model cancellation and prompt list clearing on a provider-only switch; independently falsify provider change, endpoint change, and needless invalidation when neither changes. Do not remove the guard because a mutant survives it. NB20 has landed and superseded none of this property: the comparison reads one resolved configuration value rather than two fields, and its two halves are unchanged.

#### NB26 — A test factory mode can create a manager that reaches the real network

**Validated — source inspection.** `TestNetworkFactory.ManagerSessionConfiguration.productionDefault` passes `nil` to `TTSNetworkManager.init(configuration:)`, so that manager builds `productionSessionConfiguration()` with default protocol classes, and nothing registers `MockURLProtocol` globally. Only a comment — "no request may be issued from that manager" — keeps a test in that mode off the network, which AGENTS forbids outright. The one current caller issues no request from it. **Paths:** [TestNetworkSupport](Tests/TestNetworkSupport.swift), [session policy test](Tests/TTSNetworkManagerSessionPolicyTests.swift). **Direction:** make the mode structurally unable to leave the machine — layer a protocol class that fails every request onto the inspected policy, or assert the policy `productionSessionConfiguration()` returns without building a live session from it. **Acceptance:** the five policy assertions still hold against what the manager actually used; a request issued from that manager fails locally; the mock-routed modes and scope teardown are unchanged.

#### NB27 — A later malformed Gemini inline part discards audio already validated ahead of it

**Validated — source inspection.** `audioPayload(in:)` now reads every part before returning, so any part whose `inlineData` is unreadable or declares a non-audio media type makes the whole candidate `.invalid` and takes the fatal revocation path, dropping audio parts that passed validation earlier in the same event. Before `912bf09` the first audio part returned immediately and a later part was never read. The trade-off is deliberate and documented; the exposure is that a provider adding one non-audio inline part to an `AUDIO`-modality response would silence an utterance that previously played. **Paths:** [audioPayload](Sources/Managers/TTSNetworkManager+GeminiStreaming.swift), [Gemini streaming tests](Tests/TTSNetworkManagerGeminiStreamingTests.swift), README. **Direction:** decide from provider evidence whether an unplayable part accompanying valid audio is a corrupt stream or an ignorable extra; keep the current rule until that evidence exists. **Acceptance:** the chosen rule and its evidence are stated in README, and both a malformed-only event and a mixed valid/malformed event keep a test.

#### NB28 — Three landed invariants carry no recorded mutation evidence

**Validated — TODO inspection.** The NB2, NB3, and NB7 dispositions describe the new behavior but record no mutants, while NB1, NB4, NB5, NB9, NB10, and B2–B4 each name the mutants that fail a named test. AGENTS requires a revert, a plausible regression, and an over-restriction where applicable for each added or changed invariant. Inspection indicates the obvious reverts do die — the parity predicate against `testOpenAICompatibleResponsesKeepACompleteFrameBeforeTrailingPartialBytes`, first-part-only Gemini delivery against `testGeminiDeliversEveryAudioPartInOrderAndJoinsPartialFrames`, and endpoint inference against `testCustomGoogleLookingHostUsesOpenAICompatibleModelDiscovery` — but that is reasoning, not a run. **Paths:** the NB2/NB3/NB7 dispositions below, [PCM completion](Sources/Managers/TTSNetworkManager+Failures.swift), [Gemini streaming](Sources/Managers/TTSNetworkManager+GeminiStreaming.swift), [provider dispatch](Sources/Managers/TTSNetworkManager.swift). **Direction/acceptance:** run the missing mutants in isolated scratch copies and record each against the named test that fails it, or record why a category does not apply. Classify on a named failing test rather than an exit code or a test-count summary.

### Nits

#### N7 — The automatic-playback prebuffer closure draws an implicit-strong-capture warning

**Validated — clean build output.** The clean `check-coverage.sh` build for NB12 (`6578dac`) reported `'weak' ownership of capture 'self' differs from implicitly-captured strong reference in outer scope [#ImplicitStrongCapture]` at [AudioPlayerManager.swift:275](Sources/Managers/AudioPlayerManager.swift). The inner `[weak self]` scheduler closure sits inside `scheduleAudio`'s `bufferQueue.async` closure, which captures `self` strongly without saying so. The line dates from `503ab88b`, and NB12 did not touch the file. It was not previously recorded, and whether Xcode 26.6 emitted it is unverified. **Paths:** [AudioPlayerManager](Sources/Managers/AudioPlayerManager.swift), [audio tests](Tests/AudioPlayerManagerTests.swift). **Direction:** make the capture intent explicit so the warning resolves without suppression. Keep the prebuffer closure from retaining the manager across its delay. **Acceptance:** a clean build reports no source warning at that site; automatic playback, its generation guard, and B3's pause revocation behave as before; no diagnostic is disabled.

## Ready for implementation

No task is currently expanded into a full boundary. Expand every assigned task into a boundary here before implementing it, and give every change its assigned scope.

## Deferred, accepted, and completed dispositions

- **NB20 (resolved request configuration) — Fixed (D18).** The configuration future requests read is now one `RequestSettings` value under `stateQueue`, in place of the five mutable fields that held the same thing — `baseURL`, `apiKey`, `model`, `voice`, and `selectedMetadataProvider` — and `updateSettings` replaces it whole rather than field by field, so no request can be built from a half-applied edit and no field can be left behind by one. `TTSNetworkManager.RequestSettings.resolved(provider:apiKey:defaults:)` is the pure function that decides what a launch starts with: the initializer no longer switches over providers to read their stored defaults, and what each provider starts with can be stated without building a manager or a session. The key is passed in rather than read there, because migrating and reading a saved key is `APIKeyStartupState`'s decision and a second read is what that type exists to avoid. Only a Custom deployment's endpoint comes from storage; OpenAI's and Gemini's stay fixed in source. The metadata freshness check reads that one value in its own `stateQueue` transaction, which is why it stays readable inside the type rather than becoming private — a cross-file extension cannot see a `private` property, and both fields it replaced were already `private(set)` for the same reason. Nothing outside the type reads it: every other reader goes through `requestSettingsSnapshot()`, `currentModel()`, or `isCurrentProvider(_:)`, which take the lock. Fifteen mutants were run against eight suites in an isolated scratch copy, against an unmutated control that passed, and fourteen fail a named test: reading another provider's model and voice keys, ignoring the stored Custom endpoint, giving Gemini OpenAI's endpoint, taking OpenAI's endpoint from storage, dropping OpenAI's model or Gemini's voice fallback, inventing a Custom model default, reading the key from plaintext preferences instead of taking the supplied one, resolving the startup configuration for a fixed provider rather than the persisted one, replacing the configuration while keeping the previous voice, and comparing the metadata scope after the replacement rather than before it. Three of the fifteen survived those eight suites and were investigated rather than dropped: rerun against the whole suite, dropping the endpoint from the invalidation comparison fails `TTSNetworkManagerMetadataLifecycleTests.testScopeChangesCancelThePendingModelRequest`, and invalidating for every edit — the over-restriction — fails `SettingsVoiceMetadataTests.testTypedOffCatalogModelAndVoiceStaySelectedBesideTheDocumentedCatalog`. **Limitation:** the fifteenth, dropping the provider from that comparison, survives the whole suite at this revision, which reproduces NB25's inherited claim with current evidence rather than leaving it historical. The comparison itself is unchanged by this change, and NB25 owns closing it. README owns the current behavior.
- **NB20 (provider catalogs and discovery) — Fixed (D18, D19).** `ProviderMetadataCatalog` now states what each provider documents — OpenAI's two voice sets with the model rule that chooses between them, Gemini's thirty voices in the guide's order, and the one Gemini TTS model that needs no discovery request — and `OpenAIModelDiscovery` owns the other half of that contract: the models endpoint a speech endpoint implies, refused when its transport would expose the key it carries, and the payload whose `tts` identifiers are offered. Both are pure, so a drifted catalog, a derived endpoint, and a payload shape are now stated and checked without a manager, a session, or a main-queue turn, and each reads 100% under the gate. `TTSNetworkManager+Metadata` keeps what decides whether an answer may still be shown — the token guard, the credential, and the publication — and lost 54 lines and the nested response model with them. The `nesting` exception in `.swiftlint.yml` had named that model as its reason, and now names all three declarations that actually use it — the speech request body's coding keys, one Gemini event's audio classification, and the discovery response's own model — which a `type_level: 1` probe on 2026-09-21 reported as its only consumers. A provider that documents no catalog answers `nil` rather than an empty list, which is what leaves a Custom endpoint's suggestions `.unpublished` instead of publishing an empty catalog under its name. **Assumption recorded in the boundary:** the configured model is now read for every provider rather than only for OpenAI — one extra `stateQueue` read per settings refresh — because which catalogs depend on the model is the catalog's own contract rather than its caller's. No existing test changed, so the manager suites still own the wiring, which is what fails when the caller stops passing the configured model, publishes a list for a provider that documents none, or stops publishing a documented one. Sixteen mutants were run against six suites in an isolated scratch copy, against an unmutated control that passed, and each fails a named test. Reverts: returning the current OpenAI voices for the two legacy models, dropping a Gemini voice, answering `[]` where a provider documents no catalog, documenting models for every provider, making Gemini's models need discovery, asking the speech endpoint for the model list, skipping the discovery transport guard, offering every discovered model rather than the `tts` ones, and reading an unreadable payload as an empty catalog. Three more revert the caller's wiring: passing a constant model instead of the configured one, publishing an empty list where the catalog says none, and dropping the documented-models branch. Over-restrictions: treating every `tts`-prefixed model as legacy, folding case before the legacy match, reading an empty catalog as unreadable, and refusing an endpoint that names no `/audio/speech` path. The pure suites kill the contract mutants and the manager suites kill the three wiring ones; two of the caller mutants are killed by `TTSNetworkManagerMetadataProviderTests.testVoiceCatalogsReflectProviderAndModelCapabilities` alone, which is why that suite's provider arms were left in place rather than replaced by the pure assertions. Re-verifying either provider's documented set against its guide is a provider-contract change and was not performed here. README owns the current ownership split.
- **NB16 — Fixed (D7, D15, D16, D17).** Every Swift source the app compiles is now gated application logic, and what is excluded is named symbol by symbol rather than by directory, so moving a file cannot change whether it is measured. Two verdicts must both hold at 85%: `Sources/Managers/` keeps a floor of its own beside the new one over the whole gated population, because a single combined number would have let the high-coverage views absorb a Managers regression down to roughly 78% before it moved. On this candidate, frozen against `41090c2`, they read 98.65% and 97.80%. The declarative-body rationale that had exempted `Sources/Views/` is gone, reconciled with what the report actually measures: the views run at 93–100% under the hosted suite, and the logic that had fallen under `Other` — `APIKeyProvider.keychainAccount`, `BundleAboutMetadata`, `AboutAction`, `InMemorySecretStore`, the migration and startup services — is gated like everything else. Three platform adapters are excluded, 76 executable lines: `AppKitMenuAlertPresenter.presentAlert(title:message:)`, which activates the app and runs a modal `NSAlert`; `StandardAboutPanelPresenter.showAbout(applicationName:applicationVersion:)`, which orders AppKit's standard About panel; and `KeychainSecretStore`'s four members, which reach the developer's real Keychain. An exclusion matches by substring, so the closures the compiler emits inside a symbol are excluded with it, and both of its counts leave the ratio. Per-function records make that arithmetic exact: their sums were verified on 2026-09-20 to equal their file totals for all 26 measured files, with unique names. NB15's fail-closed properties carry over to the wider population and gained their own: an exclusion that matches nothing in the source it names, a source the report does not measure, an unreadable function record, two exclusions claiming one function, and a source carrying an exclusion whose function records do not sum to its own counts are each a refusal, as is a listed zero-executable-line source — `Sources/SettingsKeys.swift` is the only one — that gains lines or leaves the repository. `verify-coverage-gate.sh` became `verify-coverage-gate.py` in the same change, because its fixture builder was already Python inside a heredoc and its case table grew here; it drives the same real script end to end over forty-two synthetic reports and now separates stdout from stderr, so a refusal must reach stderr and print no verdict. Forty-three mutants were run in an isolated scratch copy against an unmutated control that passed, and each fails a named case: gating only `Sources/Managers/` again, dropping either verdict, counting the excluded lines in the ratio, subtracting them from the denominator alone, matching an excluded symbol by equality rather than substring, searching every source for it rather than the one it names, and reverting the stale-exclusion, function-list, unreadable-function, double-claim, gated-line, listed-source, unlisted-source or Managers-population guard; together with NB15's reverts and regressions over the wider population, and over-restrictions requiring every record to carry functions, requiring an excluded symbol to be wholly uncovered, and refusing a listed source the report happens to name. Two survived an early run and were investigated rather than dropped: no case had pinned that an excluded symbol's covered lines leave the numerator, now pinned by a case asserting `SecretStore.swift` reads 79.00% when its excluded symbols carry 16 covered of 47, and none had removed a listed source from disk. Applying the function-sum check to every source carrying function records, rather than only to the sources an exclusion names, survived until the second review pointed out that the boundary is itself a property: the gate never reads those records, so a source no exclusion names may carry records that account for nothing, and a case now says so. A fourth review found that the verifier forbade a verdict only in the refusals' stdout, so a gate printing one to stderr survived all its cases; both streams are now checked, and that mutant fails eleven of them. Its Non-blocking finding, fixed here by user decision, was that README described a zero-executable-line source as carrying no record at all, while the gate accepts either no record or one carrying no lines, as a case already pinned. A third review found no Blocking defect and one Non-blocking one, which the user chose to fix here: the threshold was compared in binary floating point with a 1e-9 fudge, so a crafted report whose true ratio was 84.9999999999999999% passed as 85.00%, and counts beyond floating point raised an uncaught error and exited 1 rather than refusing. Both counts now stay integers — the verdict compares `covered * 100` against `85 * executable`, and the displayed percentage is truncated rather than rounded — so a ratio below the threshold fails however close it is, one that reaches it passes, and no report is too large to judge. The second review also found that the smoke checks named flows that do not reach their adapters: `presentAlert` has one caller, the refusal of OpenAI clipboard text over 4,096 characters, and About is a Settings sidebar control rather than a menu item. README now names both. The first adversarial review of this change found two Blocking gaps, both reproduced in a probe. The subtraction trusted an excluded function's own line count: a report whose three exclusion-bearing sources each overstated one excluded symbol by five executable lines subtracted uncovered lines the files never carried and reported 84.49% where the true ratio was 84.00%, which at the boundary is the difference between a refusal and a pass. A source carrying an exclusion must now have its function records sum to its own two counts, which makes the remainder exactly the functions no exclusion claimed, and that retired the weaker guard which had only caught an impossible remainder. The review also found that the Keychain smoke check covered saving and reading but not `KeychainSecretStore.deleteSecret`, which clearing a key in Settings reaches through `SettingsSecretState.saveSecret`; the documented procedure now clears the key and relaunches as well. Both are now cases. **Outstanding (D17):** the three exclusions' end-to-end smoke checks — a menu speech failure showing the alert `MenuBarView` built, About showing the application's name and version, and an API key saved in Settings surviving a relaunch and, once cleared, staying gone, since clearing is what reaches the excluded `deleteSecret` — are documented in README and have not been run by the agent. Run them before relying on those exclusions. **Limitation:** shellcheck is still not installed on this machine, so `check-coverage.sh` was checked with `bash -n` only. README owns the current policy.
- **NB15 — Fixed.** `check-coverage.sh` now enumerates the gated sources on disk and compares a percentage only once the report has been found to measure exactly that population: every gated source present, none counted that is no longer on disk, none counted twice, and each carrying at least one executable line. A population it cannot measure exits 2 — the gated directory absent from disk, the target absent from the report, a gated source omitted, counted stale, duplicated, or measured at zero executable lines — while coverage that was measured and fell short still exits 1, and a refusal prints no PASS verdict. A report the script cannot read reaches that same exit 2 rather than the threshold's: a failed extraction, a body that is not JSON, and targets, a file list, or a file record that is not the shape xccov documents. A record is read for its path and its two line counts only, and its name comes from the last `/Sources/` component of that path; counts it cannot mean — a boolean, a negative, or more covered lines than executable ones — are refused there rather than averaged, while a fully covered and a wholly uncovered file stay ordinary readings. A group with no executable lines reads `n/a` rather than `100.00%`, which was the reading that let a 0/0 population pass. The exemption is not tightened: an exempt source may carry no executable lines, name a file no longer on disk, or be absent from the report, as `Sources/SettingsKeys.swift` is, declaring only string constants and appearing in no coverage record. The script additionally accepts a path ending in `.json` as an already-extracted report, and `verify-coverage-gate.sh` uses that seam to drive the real script end to end over twenty-seven synthetic reports with no build, bundle, or network. The first adversarial review found two Blocking gaps in the first candidate, both reproduced in a probe: the verdict summed every manager record in the report rather than the enumerated population, so a stale record for a deleted manager passed at 98.43% while all sixteen current gated sources sat at 0/1; and a report that could not be parsed exited 1 through an uncaught exception, which is the code that means measured-but-insufficient. Its Non-blocking finding — that `split("/Sources/", 1)` read the first `Sources` component, so a checkout under a `Sources`-named parent was refused as though every gated file were absent — was fixed in the same change by user decision on 2026-09-20. A second review then found that the record validation accepted counts no report can mean: `-1/-1` and `true/true` each passed at 100% and `2/1` at 200%, because Python reads a JSON boolean as an integer and nothing bounded the two counts against each other. Twenty-seven mutants were run in an isolated scratch copy against an unmutated control that passed, and each fails a named case: reverting the missing-source, stale-source, duplicate, zero-executable-line, empty-population, JSON-parse, targets, file-list, record-object, record-path, record-count, count-range, or extraction guard; reverting the boolean rejection, the `n/a` reading, or the last-`/Sources/` normalization; enumerating a different directory, an enumeration matching nothing, leaving the enumerated `Sources/` prefix in place, and letting a failed enumeration abort rather than fail closed; running the four population guards after the verdict instead of before it; and, as over-restrictions, applying the zero-line check to every group, requiring every source rather than every gated source to be measured, refusing any record whose path is not on disk, requiring a record to carry fields beyond the three the gate reads, and requiring a file to be partially rather than fully or wholly uncovered. Which files are gated is unchanged and stays with NB16, whose acceptance now has this property to preserve. **Limitation:** the two scripts were syntax-checked with `bash -n`; shellcheck is not installed on this machine. README owns the current policy.
- **NB8 — Fixed (D14).** Every request-state publication now goes through `publishRequestState` on the main queue. While one is being stored, a publication an observer triggers there, such as a Stop's clear and `isStreaming = false`, is queued unevaluated. The outer publication applies the queue in order right after its own store, before it returns. Each queued publication's generation check and message comparison therefore judge the state it replaces, and `objectWillChange` announces it after the outer value is stored. Revocation stays synchronous, and an observer-started request is still deferred to the next main-queue turn. The former publication-depth counter is now a flag, because a queued publication cannot nest. No production observer was shown to trigger the defect; SwiftUI observes after the store. `ReentrantPublicationTests` holds the state to D14 without draining the main queue. Fourteen mutants were run against four suites in an isolated scratch copy, with an unmutated control that passed. Thirteen each fail a named test: applying nested publications inline, either everywhere or in any one of the five publishing methods; deferring them to the next main-queue turn; applying the queue last-in first-out; never applying it; discarding nested publications; checking a failure's generation or the read-failure message when a publication is queued rather than when it applies; and deferring Stop's revocation along with its publications. The first review found that an inline migration warning survived. The migration regression and the check that a stale withdrawal announces nothing were added for it. Clearing the flag after the drain rather than before it survives and is equivalent. Between drained publications, nothing on the main queue reads the flag or notifies an observer, and each drained publication sets the flag around its own store. Off the main queue, only a concurrent request start reads it, and deferring that start to main or proceeding are both permitted. README owns the current behavior.
- **NB6 — Fixed (D13).** `GeminiSSEEventParser` now keeps a cursor over the bytes it has already searched. Each received byte is searched for a line ending once, the consumed prefix is dropped once per packet, and an event's data lines are joined in linear time. One event may occupy at most 64 MiB before it ends. It is charged the raw bytes of its data lines, `data:` included, plus any line still unfinished, without line feeds. The charge is checked as each line completes and between callbacks, so where URLSession splits the stream cannot change whether an event fits. An empty data line costs five bytes, and the charge is never smaller than the joined payload. An event over the cap fails the stream as the packet arrives, on the existing malformed-event revocation path. The cap's evidence was re-verified 2026-09-18 and is recorded in D13 and README. Google's model pages give 16,384 output tokens, and its pricing page bills 25 audio tokens per second, so the largest documented event is about 42 MB. The speech-generation guide says only the 3.1 model streams TTS, so a 2.5 model sends that whole event at once, and the old per-packet rescan was quadratic on real traffic. The first adversarial review found two Blocking gaps in the first candidate, and both reproduced in a probe. The cap was checked only after a callback, so an event ending in that callback escaped it. Only data values were charged, so empty data lines grew an event without bound. Thirteen mutants were run against the three Gemini suites in an isolated scratch copy, with an unmutated control that passed, and each fails a named test. Searching from the line start again fails both search-count tests. Removing the cap, either of its two checks, or the current line from the in-loop check each fail a named test. So does making either check strict, charging only values, or not charging data lines at all. Leaving the charge unreset, lowering the cap to 32 MiB or raising it to 128 MiB, or swallowing the refusal in the caller each fail one as well. The manager-level tests hold the mock response open, so end of stream cannot fail the event with the same message instead. The over-cap event they finish is well-formed JSON, so a missed cap would deliver audio rather than fail as malformed. They state D13's 64 MiB rather than reading the production constant, so raising the cap fails them. Earlier versions let mutants survive, and a second review found that the raised-cap mutant had not been run. Per-packet prefix removal and the linear join carry no mutation claim, because time is their only symptom. Fields other than `data`, and memory used after an event completes, remain out of scope. README owns the current behavior.
- **NB29 — Fixed (D12).** `ClipboardTTSApp` now targets macOS 14, the same minimum as its test bundle, so every gate runs on the app's own floor and no macOS 13 claim remains to evidence. Raising the target made the single-argument `onChange(of:perform:)` deprecated, so the form's five observers use the macOS 14 zero- and two-argument forms with their default `initial: false`, which keeps them from firing on mount. Fifteen mutants were run against the full suite in an isolated scratch copy, against an unmutated control that passed. Nine each fail a named test: dropping the provider, endpoint, sample-rate, model, or voice callback, passing the sample-rate observer its old value, and firing the provider, sample-rate, or model observer on mount. The endpoint drop and the two mount-time mutants on the provider and sample-rate observers first survived; `SettingsChangeObservationTests` now covers an endpoint edit reaching future requests, one discovery per open, no invalid format published while the form mounts, and a fractional saved rate surviving a mount while speech plays, which a whole-number draft and a draft filled from the default rate each fail. A probe showed that the sample-rate observer firing on mount judges an empty draft before the saved rate replaces it. For the floor itself, `DeploymentTargetTests` reads both bundles' `LSMinimumSystemVersion` and fails a test floor of 15 alone or with the app's; an app floor of 15 alone, or of 13, fails the build instead. Each floor mutant was built from empty derived data. **Tooling note:** the first coverage run after the target change crashed every hosted Settings test in `SettingsView` value copies, from stale incremental products; the same suite passed from fresh DerivedData, and the gate passed after clearing the project's DerivedData. README owns the current platform statement.
- **NB13 — Fixed.** Settings offers **Retry Reading Saved Keys** while any saved key could not be read — a separate control, with no automatic re-read, by user decision on 2026-09-18. `SettingsSecretState.retryUnreadableSecrets` rereads only providers still unreadable, so a key the user stored since is never replaced, and it adopts each key it can now read, counting "no key saved" as a successful read. It uses the path a secured migration already uses, so only that provider's read guidance gives way to the next unresolved one and an unfixed save failure stays. The form then hands the credentials to future requests and, when the selected provider's key changed, rediscovers models without republishing voices. The manager withdraws its startup read-failure message for a recovered provider only while that exact message is still shown. Both recovery controls moved into `SavedKeyRecoveryView` because `SettingsView.swift` was at the 400-line `file_length` limit; the migration section is unchanged. Out of scope: typing a key does not clear the menu's startup read failure, which the next speech request or stop clears. Sixteen mutants were run in an isolated scratch copy against an unmutated control that passed, and each fails a named test in `SavedKeyReadRecoveryTests`: a no-op retry, never or always rendering the button, dropping the credential handoff or the withdrawal, treating an absent key as a failure, rereading every provider, retrying only the first, withdrawing unconditionally or any provider's read failure, clearing all guidance on recovery, keeping a recovered provider unreadable, reporting a still-failing one as recovered, and never, always, or fully resyncing instead of rediscovering. The withdrawal's off-main dispatch branch is uncovered, as its migration sibling's already was, because Settings calls it on the main thread. README owns the current behavior.
- **NB14 — Fixed.** Each model and voice row in `ModelVoiceConfigurationView` now observes its binding once rather than once per control, so typing and choosing from the picker each apply an edit once. `SettingsView.syncSettings` now takes flags for which lists to refresh. For OpenAI and Gemini, opening the window and provider and API-key edits still rediscover models and republish the voice catalog. A Custom endpoint edit applies the configuration without discovery, as before. A model edit republishes only the voice catalog, after the manager holds the new model, because only the OpenAI catalog follows the model. A voice edit refreshes neither. Assumption recorded in the boundary: a failed discovery is no longer retried by typing a model, only by reopening Settings or editing the provider, endpoint, or key. The manager's metadata guards and invalidation are unchanged. Thirteen mutants were run in an isolated scratch copy against an unmutated control that passed. Each fails a named test in `SettingsMetadataRefreshTests`: restoring either picker's duplicate observer, running a full sync for a model or a voice edit, skipping the model edit's catalog refresh or running it before the configuration is applied, applying nothing for either edit, observing only through the pickers, dropping the key edit's discovery, turning discovery off by default, swapping the two callbacks, and reporting a model change as a voice change. README owns the current behavior.
- **N6 — Fixed.** `displayedCustomSampleRate` is separated from its neighbours again. The entry's claim that no lint result changes was wrong: `SettingsView.swift` was at exactly SwiftLint's 400-line `file_length` limit, so the blank lines only fit after NB14 shortened the file.
- **NB12 — Fixed.** `APIKeyStartupState.load` now takes the selected provider's key from what the launch migration secured, and reads the store only when the migration secured nothing for that provider, which is how Settings' migration retry already adopts `securedSecrets`. A key moved out of plaintext therefore stays usable even when a second read would fail. Keychain-over-legacy precedence stays with the migration. Pending-migration guidance still outranks the startup read failure, and a launch with nothing to migrate reads and reports as before. The regressions use `ScriptedSecretStore` to refuse the read-back, and the two composed-startup tests now expect the injected store to see only the migration's read and save. Six mutants were run in an isolated scratch copy, and each fails a named test: reverting to the second read, adopting the first secured provider rather than the selected one, preferring a fresh read over the secured value, dropping another provider's pending guidance, never reading when nothing was secured, and dropping the startup read-failure message. README owns the current behavior. NB13 later gave Settings' own reads a retry.
- **NB11 — Fixed (D10, D11).** NB11a made an engine-start failure its own error, separate from the format error. Every start now publishes through one owner that clears the failure on success, and a new session retries a stopped engine instead of refusing. NB11b makes the player observe `AVAudioEngineConfigurationChange` for its own engine, on the main queue. A change pauses the session at its position the way Pause does, restarts the engine, and reschedules the remaining PCM from there. A Play that has to start a stopped engine reschedules the same way. The change is counted on the posting thread, so an automatic start or underrun resume already queued on the main queue stands down until recovery runs. An underrun resume also never plays a stopped engine's node. README owns both contracts. **Limitation:** automated tests use an owned engine and notification center. A physical output-device switch and macOS 13 were not exercised. **Revisit if** a real device change shows the engine does not stop, or needs its graph reconnected for a new hardware format.
- **NB10 — Fixed.** Settings now hydrates Custom sample-rate text from `Double`'s round-trip representation, removing only a terminal whole-number `.0`. A valid fractional rate therefore survives mount and reopen unchanged rather than being saved back through an integer draft or rebuilding the graph at a different rate. The hosted owned-defaults regression reads the field without editing it, asserts its 24000.4 draft, persistence, and graph immediately after mount and reopen, and proves an unrelated ordinary settings sync preserves the same rate. Reverting the integer formatter, rounding to three significant digits, restricting rates to integers, and rounding the synchronized target each fail it in isolated worktrees. README owns the current behavior.
- **N1 — Fixed.** Completed documentation-only boundary — **intent/direction:** align [TTSNetworkManager](Sources/Managers/TTSNetworkManager.swift)'s count with its four listed rules; **dependency:** that source list; **non-goals:** change no rule, test, or runtime behavior; **validation:** inspect the comment, local TODO link, and diff; **done:** remove the active entry and retain this trace.
- **N2 — Fixed.** Updated [.swiftlint.yml](.swiftlint.yml)'s function-length rationale to describe `streamTTS`'s current validation, authorization, and lifecycle setup; no lint rule, threshold, source, test, or runtime behavior changed.
- **NB2 — Fixed.** Completion now treats a response as playable when it contains at least one complete 16-bit PCM frame, so a final orphaned byte no longer turns the frames the player schedules into a no-audio failure. Empty and single-byte OpenAI, Custom, and Gemini responses still fail; Gemini continues delivering only its complete frames, and the player continues scheduling only complete frames. README owns the current behavior.
- **NB3 — Fixed.** The selected Gemini candidate now validates every `inlineData` part before queuing any of its PCM, then contributes those parts in source order through the request-owned frame accumulator. A valid multipart event therefore preserves split Int16 frames, while a malformed later part still takes the established fatal revocation path instead of silently succeeding. Candidate selection and metadata-only events are unchanged. README owns the current behavior.
- **N3 — Fixed.** The Settings sidebar provider binding now calls `syncSettings()` directly, preserving provider normalization, future-request settings, audio-format synchronization, and metadata refresh. NB14 later removed the duplicate model/voice observers and limited discovery to the edits that can change it.
- **N4 — Fixed.** Renamed the three metadata-provider tests for their protected provider/catalog behavior; discovery, declaration placement, bodies, assertions, fixtures, and production behavior are unchanged. XCTest executes them alphabetically under their new names.
- **N5 — Fixed.** The test bundle now targets macOS 14, matching Xcode 26.6 XCTest's binary minimum; the app remained macOS 13 until D12 raised it to 14 (NB29). `check-coverage.sh` uses the generated app scheme and its host architecture. Regeneration was inspected but remains uncommitted; a clean 294-test run and the 97.79% coverage gate passed. AppIntents' skipped-metadata messages are benign because neither target links its framework; no source warning was suppressed.
- **NB5 — Fixed.** Production sessions now start from one explicit ephemeral policy with no URL cache, cookie storage, or credential storage, and use `.reloadIgnoringLocalCacheData`; test routing layers its protocol onto that policy without changing its scope teardown. The focused regression inspects the effective default session and sends a request through that mock-routed policy. Reverting the default, restoring the cache or stores, selecting `.reloadIgnoringLocalAndRemoteCacheData`, or dropping the policy supplied to test routing each fail in an isolated scratch checkout. README owns the current policy.
- **B1 — Fixed.** Tests own the settings storage they state; `SettingsView` binds every `@AppStorage` to the domain it is handed, and the mock-network lifecycle no longer snapshots, clears, or restores the app's own domain. Two isolation tests name the domain by identity, but no runtime test observes which domain an ordinary call site was handed, so that guarantee is held by the compiler (no settings API in `Sources` defaults to `.standard`), by `InMemoryDefaults` owning its inherited surface, and by the `process_default_settings_store` and `test_owned_settings_storage` lint rules over the named routes in `Sources` and `Tests`. README owns the current contract. NB17 and NB18 build on this ownership rather than reopening it.
- **B2 — Fixed.** The progress tick still stops playback at the end of the buffered PCM, but `AudioPlayerManager.stopAtBufferedEnd` now records why, and only one of the four reasons is undone by the PCM that arrives behind it. A stream whose request has published `streamTermination` has been heard to its end and stays stopped; a stream still open has run dry, so `underrunSuspendedGeneration` names it and the next chunk `scheduleAudio` puts on the node resumes it in the same main-queue turn that publishes the grown buffer. The node was paused rather than stopped, so resuming plays on from its own sample time: nothing already heard is rescheduled and no second prebuffer window opens. The tick discounts however far the rendered position overshot the buffered end while the node ran dry, because that silence is not audio anyone heard and carrying it forward would put each resumed position a further tick ahead of the sound and stop the next underrun early. The menu's Pause, a seek, `stop()`, and a replacing stream each release the suspension, so a decision the user made is never reversed by late audio. Whether playback has reached the end is decided against that serialized frame count rather than the published `bufferDuration`, which lands a main-queue turn after the frames it counts reach the node: judged against the lagging value, a stream already rendering its next chunk looks as though it ran dry, and the frames it had heard of that chunk would be discounted as silence. `isNodePlaying` reports what the player node is actually doing so tests can hold the published state to it; nothing in the app reads it, because the node is never the app's source of truth for what is playing, but a resume that published playback without restarting the node would otherwise satisfy every assertion. Seventeen mutants were run: reverting to an unconditional final stop, treating a terminated stream as resumable, making Pause resumable, letting a seek keep the suspension, dropping the offset re-anchor, resuming without the progress timer, resuming by re-seeking, resuming on every chunk, failing to consume the suspension, deciding the end against the published duration, anchoring to it, requiring the rendered position to pass the end rather than reach it, resuming without restarting the node, and suspending without pausing it each fail a named test. Removing `stop()`'s clear and removing the suspension's generation comparison each survive alone and are equivalent by construction — `stop()` only ever advances the generation, so a value left behind can never equal a later stream's — while removing both fails the replacement-stream test; both are kept, matching how the sibling automatic-playback generations are cleared. `AudioPlayerManager` gained an extension owning this end-of-stream state machine, because the class body was already at its `type_body_length` limit and these members are one responsibility rather than a split for length. README owns the current contract. NB9 later paired a live format change with its request's cancellation.
- **B3 — Fixed.** `AudioPlayerManager.pause()` records the paused stream's `scheduleGeneration` in `automaticPlaybackSuppressedGeneration`, so the pending automatic start the prebuffer deadline releases finds the intent it would otherwise reverse. The revocation is bound to the stream that was paused and released by `stop()`, so `play()` still resumes that stream and a later stream keeps its own deadline. Pause does not retire the scheduling generation, so the paused stream keeps buffering the PCM that arrives behind it; a regression pins that, because revoking by retiring the generation otherwise satisfies every other pause assertion while truncating the audio. README owns the current contract. B2 later built the end-of-playback state machine on this revocation, routing the tick's own stop through the same path.
- **B4 — Fixed.** `AudioPlayerManager.applyProgressTick()` reads the node's rendered position and publishes the position it implies in one main-queue turn, and the repeating timer does nothing but call it, so no rendered position is held across a turn in which `seek` can install a new `baseProgressOffset`. The tick keeps the clamp and the pause it performs on reaching the buffered end, and still publishes nothing when the node reports no rendered position. It is internal so its tests drive it as the timer does; the position it reads comes from an injected reader, and the manager builds its own repeating timer while an injected scheduler decides where that timer runs, because a test can otherwise neither state what live audio has rendered nor place a tick without waiting a cadence out. Reaching the buffered end paused through `pause()` at the time, so the tick also revoked the prebuffer start pending behind it; B2 later moved that stop onto a path that records why playback stopped, and it still performs the same revocation. Mutants that defer either the publication or the whole tick reproduce the 1.8-to-2.0 advance, and ones that drop the clamp, the missing-reading guard, the timer's call into the tick, its scheduling, its invalidation, its cadence, or that end the stream without `pause()`'s revocation each fail a test. README owns the current contract. B2 later gave the tick's stop at the buffered end a recorded reason, so that stop is now the entry point to the end-of-playback state machine rather than the end of playback itself.
- **NB19 — Fixed.** `SpeechSessionCoordinator` is the one owner of a speech session, and the menu, Services, and Test Voice start and cancel through it rather than sequencing `AudioPlayerManager` and `TTSNetworkManager` themselves. It owns only what all three did identically: the refusal while the audio graph cannot play the selected format, the audio generation a session's PCM belongs to, and the binding of that request's audio and terminal event to it. The menu keeps its deferred clipboard read, idle revalidation, and OpenAI length refusal; Settings keeps its provider normalization, format validation, and credential handoff before the sample is spoken. Replacing a session takes two steps rather than three, because `startNewStream()` already discards the previous audio and `streamTTS` already revokes the previous request; cancelling in the owner as well advanced the request generation twice for one action, which the menu's deferred clipboard read reads as the number of requests a click started. A request now delivers exactly one terminal event, `SpeechStreamTermination.finished` or `.failed`, through the same delivery queue, callback authority, and generation check as its PCM, so it cannot overtake accepted audio and is withdrawn once a stop or replacement owns the pipeline; a request refused before it owns a task still terminates its session, and one whose automatic retry starts does not. `AudioPlayerManager.finishStream` applies it on the buffer queue behind that stream's own PCM and publishes `streamTermination`, which `stop()` clears. README owns the current contract. B2 consumed the terminal event this change published, and NB9 later paired a live format change with its request's cancellation through this owner: this change built the interface both need without changing what playback does.
- **NB7 — Fixed.** `APIKeyProvider` now normalizes persisted and direct strings before network state stores them, then request snapshots, active requests, metadata tokens, and suggestion ownership carry that typed identity. Only `.gemini` selects native Gemini request and catalog behavior; a Custom endpoint containing Google's hostname remains OpenAI-compatible for speech and model discovery. Provider-and-endpoint metadata freshness guards are unchanged. README owns the current behavior.
- **NB4 — Fixed.** A redirect now answers to two rules rather than one, and `TTSNetworkManager+Redirects` owns both. The target's transport is decided first, so a cleartext target still reports the endpoint failure whose remedy Settings documents, even though it is a foreign origin as well; a target that is protected but not the origin the request was built for is refused as its own failure, which names neither the configured endpoint nor the provider-controlled address it refused. `EndpointTransportPolicy.isSameOrigin` decides that origin from scheme, host, and effective port — scheme and host read case-insensitively, an omitted port as the scheme's default, and a URL it cannot read an origin from matching nothing, its own repetition included. The scheme is compared in its own right even though the default port a scheme implies usually separates two spellings already: on loopback, where both schemes are permitted and the port is stated, it is the only thing that does. A redirect the app does follow is not passed on unchanged either, because the credentials URLSession leaves on the request it builds are not the ones the app authorized: probed on Xcode 26.6 (CFNetwork 3860.700.1, Darwin 25.6.0) against a loopback server, it forwards this app's `x-goog-api-key` header and the POST body holding the user's clipboard text across a cross-origin redirect, while stripping `Authorization` from every redirect it builds, a same-origin one included — so following one unchanged both leaked a key and broke authentication on an ordinary provider path move. Each field in `CredentialHeaderField.all` is therefore restored from the request the app itself created, which puts back the one that request authenticates with and removes any other the response introduced; the request is copied rather than rebuilt, because the body URLSession replays is not visible on it — a test can hold that copy to the method and content headers it carries, and the probe is what says the body rides along with them. Both halves of the decision come from that request rather than from settings the user may have edited since it started, which is the rule the bounded retry already follows when it replays the request it sent: the origin a target is compared against, and the credential a followed target is given, are the ones the request was built with, so an endpoint saved mid-stream authorizes nothing for it and a newly typed key never reaches the origin it was not issued for. A refusal is recorded only against the speech request that owns the refused task, so discovery redirecting while speech streams leaves that stream to finish on its own response, and a refused discovery request stays as silent as every other metadata failure. Twenty-two mutants were run: removing the origin check, removing the restoration, restoring only one of the two credential fields, restoring without removing an unauthorized one, deciding origin before transport, giving both refusals one message, refusing every redirect, comparing whole URLs, ignoring scheme, host, or port, dropping the default-port normalization or misreading it, comparing scheme or host case-sensitively, giving a scheme the app never sends over a port anyway, reading a URL's own port before deciding whether the app sends over its scheme, recording a refusal against whichever request was active, taking the compared origin or the restored key from current settings instead of the request that started, and rebuilding the redirect request instead of copying the one URLSession built each fail a named test. The probe is a reproduction recipe rather than a checked-in test, and it exercised loopback HTTP only: no live provider, and no HTTPS redirect chain, was driven. README owns the current contract. NB7 still owns the endpoint-substring provider inference that decides which credential header a request carries in the first place.
- **NB9 — Fixed.** A PCM format is applied through `SpeechSessionCoordinator.applyAudioFormat(sampleRate:)`, which cancels the request paired with the audio that change discards. The two halves were always one session: the buffered PCM and the request still delivering more of it were both decoded for the rate being replaced, so retiring the audio generation alone left that request streaming bytes the player was certain to drop — spending the provider call on speech nobody can hear, and holding `isStreaming` true so the menu went on offering to clear a buffer that was already empty. Each manager still revokes its own half, as when a session is replaced: the owner cancels the request and `setSampleRate` clears the audio, so one change advances each generation once. The request is released before the graph is touched, matching the order `cancel()` uses. The order is load-bearing: rebuilding the graph takes long enough for the request to complete inside it, and a cancellation arriving after that would find nothing to cancel and leave the finished request publishing the failure of a session the format change had already retired. `AudioPlayerManager.changesAudioFormat(to:)` answers whether a rate replaces the active format, and `setSampleRate` decides with that same answer, so the prediction and the change cannot disagree. `TTSNetworkManager.stopActiveSpeechRequest()` decides and revokes in one visit to `stateQueue` under callback authority, because a request completing between a separate check and the revocation would be "cancelled" by advancing a generation it no longer owned. Ownership is the context's own generation still being the current one: a fatally malformed Gemini stream advances the generation first and leaves its context in place until the revocation waiting at callback authority collects it, and cancelling that context would advance a generation nothing owns and swallow the no-playable-audio failure it was about to publish. Published `isStreaming` is not that decision either, since it stays true for the turn between a request releasing its context and its stop being published. Ownership now covers the one permitted retry: `processCompletedTask` keeps the completed attempt's context when that attempt earned a retry, so a format change arriving during the handoff cancels the logical request and `replaceActiveRequest`'s generation guard refuses the retry, where before it found nothing to cancel and let the retry stream into a session already torn down. Releasing ownership is still what a finished request does, and every generation move still clears or replaces the context, so a refused retry leaves nothing stale behind. Nothing else may: an unsupported rate applies no format, an unchanged one leaves the session speaking, and a change with nothing paired to it leaves that generation where it was, which is what a menu click waiting out its deferred clipboard read and a failure still on screen both depend on. `SettingsView` applies every rate through the owner rather than reaching past it to the player, so both routes that can replace a live format — the Custom sample-rate field and the synchronization every other edit runs — end the session they invalidate; each route has its own hosted test, because a revert on one alone leaves the other looking correct. Thirteen mutants were run against the committed state and each fails a named test: cancelling nothing, cancelling unconditionally, cancelling on every applied rate, splitting the revocation into two request-state visits, dropping the generation from the ownership test, dropping the ownership guard entirely, revoking without publishing the release, counting an unsupported rate as a change, inverting the format comparison, bypassing the owner from either Settings route, releasing ownership before the retry installs, and cancelling after the format is applied rather than before. The last three are the ones a window hides, and each is opened deliberately rather than waited for: `TTSNetworkManager` carries two observer seams that do nothing in production — one inside the revocation's request-state transaction, one between a retry's task being created and that retry owning the pipeline — and the audio graph's existing engine starter is the third, because a completion forced from it lands while the graph is being rebuilt. An invariant about what may interleave somewhere can only be stated from inside that somewhere. Cancelling after the application is not equivalent after all, as an earlier revision of this entry claimed: the rebuild takes long enough for the request to end inside it, and a cancellation arriving afterwards finds nothing to cancel and lets that request publish the failure of the session the change had already retired. README owns the current contract. NB10 still owns the rounding that decides whether opening Settings changes a fractional Custom rate at all, and therefore whether opening the window is itself such a change.
- **NB1 — Fixed.** A success response now has to say what it is before any of it is played. `SpeechResponseFormatPolicy` owns that decision for both provider paths and reads a declaration the same way for each: parameters dropped, case folded, and a value that is not one media type refused rather than parsed. Only an absent or blank header is silence; every other value is something the provider said and has to be a whole media type, its type and subtype both HTTP tokens. A header carrying several types, which is how URLSession presents a response that declared two, has not declared what its body is, and reading only as far as its first parameter would let its leading half excuse the rest. A value carrying only parameters, and a subtype such as `()` or an emoji, are refused for the same reason rather than read as silence: since the audio tree is accepted without inspecting the subtype, whether a value is a media type at all is the last thing standing between a provider-controlled header and the player, and an adversarial review found that gap open before this landed. An OpenAI-compatible or Custom body is judged by its `Content-Type` and the accepted set is deliberately open: nothing at all passes, because OpenAI's own reference documents no content type for `/v1/audio/speech`; any `audio/*` passes without inspecting the subtype, because which one a compatible server labels raw samples with is documented nowhere and narrowing it would refuse a working deployment on a guess rather than on evidence; `application/octet-stream` and `binary/octet-stream` pass as the generic spellings D4 names. Everything else is refused, which is what closes the finding: two bytes of JSON returned with HTTP 200 were being handed to the player, and bytes that are not samples are not silently unplayable but audibly wrong, since the player reads every one of them as a little-endian 16-bit sample and renders them as full-scale noise. The refused body is dropped the way a non-2xx body already is rather than cancelled, because cancelling would end the task with a URL error and give the request a second reason to fail that is the app's own doing; `TTSNetworkManager.unplayableResponseFormatFailure` then points at the endpoint instead of inviting a retry, since a request that succeeded and returned something other than audio is answering correctly for whatever is at that address, and it quotes neither the declaration it refused nor anything else the provider sent. It is reported after an HTTP status, which stays the more actionable cause, and before a transport error, which only says that a body the app had already decided to discard stopped arriving. Gemini takes none of that rule — `text/event-stream` is the correct declaration for its transport — and is judged instead on each part's own `mimeType`, where the accepted set can be exact because one fixed provider publishes what its speech output declares: `audio/L16`, as `audio/L16;codec=pcm;rate=24000`, and `audio/pcm`, the spelling Google's Live API uses for the same samples. A part declaring anything else is a malformed event on the existing fatal path, which is right for a blob type the schema documents as carrying images, video, PDFs, and text as well. A part declaring nothing is still decoded, because the request asks for the `AUDIO` response modality alone and refusing an undeclared part would trade a defect nothing has produced for the loss of every Gemini utterance if the field were ever omitted; a `mimeType` of another JSON type counts as undeclared for the reason a `finishReason` of another type does, while a readable declaration naming other media does not. Twenty mutants were run: reverting either path's check, passing the refusal through as false, publishing no message for it, refusing an absent content type, narrowing the accepted audio subtypes, refusing generic binary, refusing an absent or non-string inline declaration, applying the response rule to Gemini as well, letting a transport error outrank the refusal, dropping the data guard that keeps the refused body out of the player, comparing case-sensitively, keeping parameters, accepting a multi-valued declaration, accepting an unreadable one, skipping token validation, reading a parameters-only value as silence, and narrowing the token set below what HTTP allows each fail a named test. Moving the refusal ahead of the HTTP status survives and is equivalent by construction: the refusal is recorded only inside the branch that has already established a 2xx status, so no completion can carry both, and the two branches cannot both be reachable for one response. The exception is a data task delivering more than one response, which `multipart/x-mixed-replace` allows and no speech endpoint sends; the flags there behave as the sibling `isErrorResponse` already does. Sample rate is not validated against the configured format, and the accepted `audio/*` set for a configurable endpoint is not narrowed: both would need evidence this decision does not have. README owns the current contract. NB2 still owns the completion predicate that decides whether the audio that did arrive counts.
- **Accepted without change — `InMemoryDefaults`' inert domain mutators (2026-09-06).** Registration, named-domain, suite, and volatile-domain mutators succeed while doing nothing, so a future production path invoking one through an injected store would look correct under test while production mutated real state. Accepted because no production path calls them, the behavior is fail-closed for B1's isolation goal, and the lint rule refuses those calls from `Tests/`. **Revisit if** production code ever calls a domain-level `UserDefaults` member through an injected store.
- **NB22 — Completed by this TODO rewrite:** the active backlog/decision register replaces the stale phase handoff. No implementation finding is marked fixed by this documentation change.
- No other implementation finding has been deferred or accepted without change.
- Legacy Tasks 14 and 36 remain withdrawn; [USER_STORIES](USER_STORIES.md) governs the fixed icon and Settings-only voice selection. This plan reinstates neither feature.

### Legacy task mapping

| Old task | Current owner | Disposition |
|---|---|---|
| 38 | NB2 / D3 | Fixed; tolerate a trailing partial byte after complete PCM |
| 40 | NB4 / D5 | Fixed; see NB4's disposition |
| 42 | NB25 | Retained validation question; historical mutant is not current proof |
| 44 | NB1 / D4 | Fixed; see NB1's disposition |
| 45 | NB5 / D6 | Fixed; see NB5's disposition |
| 46 | NB24 / D8 | Retained; remove prints |
| 48 | NB22 | Missing-rationale premise corrected: both audio files explain their exceptions; cohesion policy belongs in AGENTS, not a forced split task |
| 52 | B2 / D1 | Fixed; see B2's disposition |
| 55 | NB7 | Fixed; canonical typed identity |

## Integration acceptance

Review integrated changes against accepted decisions and current USER_STORIES/README. Do not require every non-blocking task to land first: record explicit defer/accept dispositions and reasons.

Use AGENTS for required gates/review. Supplement integration changes with cross-entry playback, cancellation, provider/format switching, failure/recovery, and settings-isolation checks. Inspect warnings and the complete gated population. Static analysis and, when relevant and supported, Thread Sanitizer provide additional evidence; clean output is not proof of race freedom.

Keep hardware and live-provider smoke tests separate from automated test dependencies. Physical switching remains unverified by this baseline; D12 retired the macOS 13 claim instead of evidencing it. Live-provider tests need explicit authorization and credentials; record no keys, copied text, or returned audio. Record evidence at its actual revision. Remove completed full entries in their implementation commits, retaining only concise dispositions/links needed by remaining work.
