# S2 — A checked-in mutation runner

## Current review — 2026-09-25 to 2026-09-26

**Boundary:** complete the existing runner without changing application behavior: prevent regeneration from erasing a mutant before testing, preserve exact file bytes (user-approved non-blocking fix), strengthen the verifier, and correct operating documentation and evidence provenance. Dependencies are the existing candidate and the passing baseline at `7f1e6289d1119590426db4ad206930d04face996`. Verdict-format redesign, other test platforms, and unrelated application findings are out of scope. Completion requires the verifier and mutation checks, application gates, fresh independent review, disposition of all findings, and owned-artifact cleanup.

**Changes:** `run_tests` compares edit-target hashes after XcodeGen and before launching tests, including the unmutated control. This refuses tracked generated outputs such as `Sources/Info.plist`; mutating their generator inputs remains supported. Planning decodes UTF-8 bytes without universal-newline conversion, applying edits preserves line endings, and restoration writes original bytes. Restoration is verified after regeneration. A refusal prints a failed tool's own error output, and a generation refusal names the changed paths and the run it preceded. README now includes a valid JSON example, output/exit semantics, and these constraints.

**First review (inherited).** The previous agent reported that a reviewer it names as GPT-6 Astra reviewed the frozen candidate after its gates passed. The review skill specifies `gpt-5.6-sol`, and no user decision authorizing that model is recorded. The reported review had one **Blocking** finding: generation silently erased an accepted tracked-file mutant before tests, producing a false `SURVIVED`. The previous agent validated it independently, and the main agent's own **Non-blocking** finding, that a dirty CRLF source was restored with LF, was fixed by user decision on 2026-09-25. The generation guard and byte-exact restore above are those fixes.

**Second review (2026-09-25).** By user direction, three Claude Opus 5.5 subagents reviewed the frozen candidate in place of the skill's Codex dispatch, each emphasizing one perspective: runner safety and state, verdict correctness and verifier strength, and documentation and process. The candidate's gates had passed beforehand, and the repository matched its freeze snapshot afterwards. Findings and dispositions:

- **Blocking, fixed.** Four invariants had no killing verifier case: mirroring a changed tracked symlink as a link rather than its target, removing a directory before a file replaces it, stopping the running test tool on a signal, and refusing a control that fails without naming a test (a crash or a log without a terminal marker). Each surviving mutant was validated by the main agent against the unmodified verifier, which passed 69/69 with each. TODO still carried S2's execution boundary beside its Done entry, and its validation named a reviewer this round did not use; the boundary is removed and this record names each round's reviewer.
- **Nits, fixed.** The module and `classify` docstrings omitted the `--out` containment rule, the full exit-1 and exit-2 meanings, and that a skipped test does not count; README omitted that `control` is reserved and that a signal before the control ends the runner by that signal; a tool failure printed only its exit status; this record had a double blank line and an unclear account of how its nine-mutant campaign selected cases; NB28 restated the classification rule rather than pointing to the runner.
- **Non-blocking, by user decision.** Correcting two statements of the inherited account below that described superseded behavior was fixed. Six were recorded in TODO as NB33–NB38: a terminal Ctrl-C can cut the post-restore regeneration short; a test that crashes, exits, or times out reads as `FAILED-UNNAMED`; nothing bounds a hanging mutant; concurrent runs can share one `--out`; `results.json` does not identify its run; and the copy could be built by one walk of the index. NB32 is not used, because the S2 rounds before this one used it informally.

**Third review (2026-09-25).** One fresh Claude Opus 5.5 subagent, by the same user direction, reviewed the fixed candidate after its gates passed; the repository matched its freeze snapshot afterwards. Findings and dispositions:

- **Blocking, fixed.** Invariants inherited from earlier rounds had no killing verifier case, and each surviving mutant was validated by the main agent against the 73-case verifier: reading XCTest's singular summaries (`Executed 1 test`, `with 1 test skipped`), whose loss would refuse a correct control as `NO-TESTS`; refusing a symlinked `copy`, `derived-data`, or `results.json` output child, where only `logs` had a case; refusing a symlinked ownership marker; and refusing a changed tracked path that is now a directory in the working tree.
- **Nit, fixed.** The generation guard's refusal named neither the run nor the changed file.
- **Non-blocking:** none. The reviewer found NB33–NB38's TODO entries accurate and not worsened.

**Fourth review (2026-09-25).** One fresh Claude Opus 5.5 subagent, by the same user direction, reviewed the candidate after its gates passed; the repository matched its freeze snapshot afterwards. Its one **Blocking** finding, fixed, was four more inherited invariants whose plausible regressions survived the 77-case verifier, each validated by the main agent: listing changed paths without rename detection, which a distinct-name staged rename needs to leave no old path in the copy; rebuilding the copy from nothing on a rerun into an owned `--out`; unlinking a symlink at a mirrored destination rather than writing through it; and matching a whole mutant name rather than a valid prefix such as `ok/x`. The main agent then swept the runner's remaining checks for mutants the verifier did not kill and found three more, now covered: an empty test selector, an empty edit list, and an `--out` that is a file. A fourth, saving originals by assignment rather than `setdefault`, is equivalent, because each mutant starts with fresh originals and its plan names each file once. The review had no Non-blocking findings or Nits.

**Fifth review (2026-09-25).** A first fresh Claude Opus 5.5 subagent stalled when the network connection dropped and returned no report; the main agent confirmed the repository still matched its freeze snapshot, found no process left running, removed its scratch directory, and dispatched a second with the same brief. That review reported two **Blocking** findings, each fixed after the main agent validated its surviving mutant: checking only ancestors above a changed path's immediate parent for a working-tree symlink survived, because the only fixture put the symlink two levels up; and installing the signal handlers only after the control survived, because every signal case fired during a mutant, although such a runner dies by the signal and leaves the control's test tool running. Its **Nit**, fixed, was that a runner outside any Git repository ended with a traceback and exit 1 rather than a refusal with exit 2. Its **Non-blocking** finding, that nothing tests that a failed post-restore regeneration stops the run, was folded into NB33 by user decision. The main agent's sweep found that creating the parent directory of a new changed file or symlink was also untested; the rename and symlink cases now move into new directories.

**Sixth review (2026-09-25).** One fresh Claude Opus 5.5 subagent, by the same user direction, reviewed the candidate after its gates passed; the repository matched its freeze snapshot afterwards. Its **Blocking** finding, fixed after the main agent validated each surviving mutant, was that the stand-in test tool recorded only the test selectors, so dropping `-derivedDataPath` (which would send build products outside `--out`) survived, and that no campaign of judged verdicts expected exit 0, so counting `BUILD-FAILED` or `FAILED-UNNAMED` as unjudged survived. The stand-in now records its whole command, which a case pins, and a campaign of `KILLED`, `SURVIVED`, `BUILD-FAILED`, and `FAILED-UNNAMED` mutants must exit 0. Two **Non-blocking** findings were dispositioned by the user: README now warns that the runner adds no isolation of its own, so a mutant that weakens the tests' isolation is not run through it as it stands; and an untested ordering, writing a mutant's texts inside the restoring `try`, is recorded as NB39. The review had no Nits.

**Seventh review (2026-09-25).** One fresh Claude Opus 5.5 subagent, by the same user direction, reviewed the candidate after its gates passed; the repository matched its freeze snapshot afterwards. Its **Blocking** finding, fixed after the main agent validated each surviving mutant, was that no case omitted `tests` or `untracked`, so reverting either default (which ends the run with a traceback and exit 1) or defaulting to the wrong bundle survived; a case now omits both and requires the whole bundle and no untracked file. Its **Nit**, fixed, was that the verifier's fixture repositories inherited the developer's Git configuration: with commit signing enabled globally, the main agent reproduced 31/84 passing, and the verifier now runs its fixtures without global or system Git configuration, passing 85/85 under that same configuration. It had no Non-blocking findings.

**Eighth review (2026-09-26).** One fresh Claude Opus 5.5 subagent, by the same user direction, reviewed the candidate after its gates passed; the repository matched its freeze snapshot afterwards. Its **Blocking** finding, fixed after the main agent validated each surviving mutant, was that no fixture held a path Git quotes, so reading any of the three Git listings line by line instead of NUL-separated survived, although a modified non-ASCII file then kept `HEAD`'s content and a staged one vanished while the run exited 0; and that nothing held the acceptance of an existing empty or new nested `--out`, so refusing either survived. A case now changes, stages, and names non-ASCII files, and another passes both kinds of `--out`. It had no Non-blocking findings or Nits.

**Ninth review (2026-09-26).** One fresh Claude Opus 5.5 subagent, by the same user direction, reviewed the candidate after its gates passed; the repository matched its freeze snapshot afterwards. It reported **no Blocking findings**. Its real runs also showed that a rerun into an owned `--out` recompiles a file restored to older content despite reused derived data. Its **Nit**, fixed and validated against the code, was that README and the module docstring said signals stop the test tool, although only SIGINT and SIGTERM are handled and a SIGHUP ends the runner at once with the mutant left in the copy; both now name the two signals and state what any other does. Its **Non-blocking** finding, that no case holds the lenient decoding of test logs and tool stderr, was recorded as NB40 by user decision.

**Tenth review (2026-09-26).** One fresh Claude Opus 5.5 subagent, by the same user direction, reviewed the candidate after its gates passed; the repository matched its freeze snapshot afterwards. Its **Blocking** finding, fixed after the main agent validated each surviving mutant, was that no case ran a mutant editing two distinct files, so writing only the first planned file survived, although it then reported a mutant whose killing edit was in the second file as `SURVIVED`, and restoring only the first file survived too. A case now runs such a mutant and requires both edits to reach the test run, a `KILLED` verdict, and both files restored byte for byte. Its **Nit**, fixed, was that the allowed name characters were documented only in the refusal message and narrowing them survived; README now states them and that case's mutant name uses `.` and `_`. It had no Non-blocking findings.

**Eleventh review (2026-09-26).** One fresh Claude Opus 5.5 subagent, by the same user direction, reviewed the candidate after its gates passed; the repository matched its freeze snapshot afterwards. It reported **no Blocking findings**. Its real runs read a failure inside an instance `setUp()` as `KILLED`, naming every test in the suite. Its two **Nits** were fixed: a verifier comment claimed that case's mutant name used every allowed punctuation character, although `-` is covered by another case's name, and this section's heading carried only its first date. Both fixes change a comment and a heading only; the verifier's syntax tree is unchanged, so they were verified by that comparison and the diff rather than by another review. Its **Non-blocking** finding, that no case holds the refusal of an `OSError` before the control, was recorded as NB41 by user decision.

**NB36 and NB37 within S2 (2026-09-26).** An external review of the candidate recommended executing NB36 and NB37 in this change, and the user decided to on 2026-09-26, authorizing `gpt-6-sol` through Codex for this task's adversarial reviews in place of the skill's `gpt-5.6-sol`. **Boundary:** a run's `--out` cannot be used by a concurrent run, and `results.json` alone says how its run ended and what it tested; NB33–NB35 and NB38–NB41, the verdict format, and the rest of the runner's design are out of scope; validation is verifier cases with mutation evidence, a real-Xcode check, the application gates, and a fresh review; done is no Blocking finding and every finding dispositioned. **Changes:** `claim_output` opens the ownership marker and takes an exclusive, non-blocking `flock` on it before anything in `--out` is deleted, and the runner holds it until it exits, so a second run on the same `--out` is refused with exit 2 and touches nothing; `O_NOFOLLOW` was considered and left out, because a symlinked marker is already refused as not ownership in a non-empty directory, so no case could exercise it. `results.json` is now an object, written however the runner ends once the control begins, except by a signal it does not handle such as SIGHUP, recording `complete`, `exit`, `error`, `head`, `spec_sha256` over the spec's bytes, `target_sha256` over the copy's relative paths, executable bits, contents, and symlink targets before generation, `tests`, `selected`, the `control` verdict, and the judged `mutants`; a run refused before its control still writes none. Refusals after the control now raise, so their message becomes the report's `error`; their stderr text is unchanged.

**Twelfth review (2026-09-26).** Codex `gpt-6-sol`, run unsandboxed through `codex exec` once the user approved that call after Claude Code's auto-mode classifier had first refused it, reviewed the candidate after its gates passed; the repository matched its freeze snapshot afterwards, and the reviewer left no artifacts. Its one **Blocking** finding was fixed after the main agent reproduced it with a new verifier case that failed on the candidate: selectors were checked only in the control, so a mutant that made one selected test skip while another ran read `SURVIVED` with exit 0. A `SURVIVED` mutant that leaves any selector without a test that ran now reads `NO-TESTS`, as the control would be refused; a kill stays a kill. It had no Non-blocking findings or Nits.

**Thirteenth review (2026-09-26).** Codex `gpt-6-sol`, dispatched the same way, reviewed the fixed candidate after its gates passed; the repository matched its freeze snapshot afterwards, and the reviewer left no artifacts. It reported **no Blocking findings** and no Nits, and confirmed the twelfth review's fix with a mutant and a focused real Xcode run. Its **Non-blocking** finding, that README and this record said `results.json` is written however the run ends although a SIGHUP after the control leaves none, was fixed by user decision as wording: both, and the runner's comment on that write, now except a signal the runner does not handle. The runner's syntax tree is unchanged, so this was verified by that comparison and the diff rather than by another review.

**Focused validation:** `./verify-mutation-runner.py` passes 91/91 on Python 3.13.9 and 3.9.6: 21 classification cases, 9 selector cases, and 61 end-to-end cases. Against this final verifier, the main agent ran 79 runner mutants, each in its own copy with the verifier unmodified, and each failed a case the verifier named: removing the generation guard, applying it only to the control or only to mutants, refusing every generated run, and reverting its refusal message or naming only the control in it; universal-newline planning; checking restoration before regeneration; following a changed symlink or refusing it; skipping or refusing directory removal before a mirrored write; not stopping the test tool on a signal, or stopping it only on SIGTERM; accepting a control that failed unnamed, or refusing only named-failure, no-test, and build-failure controls; dropping a failed tool's error output, or printing it from only the first handler; reading only plural executed or skipped summaries; dropping `derived-data`, or every child but `logs`, from the symlinked-child refusal; removing the `results.json` symlink refusal; accepting a symlinked ownership marker; dropping the refusal of a changed path that is neither a file nor a symlink; dropping `--no-renames` or forcing rename detection; reusing a stale copy; not unlinking a symlinked destination; matching a name's prefix; accepting an empty selector, an empty edit list, or an `--out` that is a file; skipping a changed path's immediate parent in the symlink check; installing the signal handlers after the control; not creating a new changed file's or symlink's parent directory; discovering the repository outside the refusal handling; dropping `-derivedDataPath`, pointing it inside the copy, or dropping `-destination`; counting `BUILD-FAILED` or `FAILED-UNNAMED` as unjudged; and removing the `tests` or `untracked` default, or defaulting `tests` to the wrong bundle; reading any of the three Git listings line by line; refusing an existing empty `--out` or creating a new one without its parents; and writing only the first or only the last file of a two-file mutant, restoring only its first, or narrowing the allowed name characters; for NB36, taking no lock, a blocking or shared one, releasing it before the run ends, or taking it only after the old logs are removed; and, for NB37, recording the tree's hash as `HEAD`, digesting the parsed spec rather than its bytes, a target digest blind to executable bits, symlinks, or contents or equal to `HEAD`, a report that is always or never complete, exit 0 on a refusal, a stale error after success, no control verdict, every spec mutant as selected under `--only`, no selectors, recording an interrupted mutant's verdict, or writing the report only on success; and, for the twelfth review, dropping the per-mutant selector check, applying it to kills as well, checking only the first selector, or reading the control's log for it. A stand-in that holds a run open for the concurrency case stops waiting once orphaned, because a lock mutant lets a second run start one that the case's timeout then orphans; before that change one was left running after the campaign and was stopped by its PID. One case stops its stand-in only when the runner does, so under a mutant that leaves it running, the stand-in exits by itself within 20 seconds. Earlier, against the 73-case verifier, the second review ran the nine mutants the first review's fixes needed and 24 further mutants of verdict reading, the third review ran 51 mutants against the 73-case verifier, the fourth review further mutants against the 77-case one, the fifth review further mutants against the 81-case one, the sixth review further mutants against the 83-case one, the seventh review further mutants against the 84-case one, the eighth review further mutants against the 85-case one, and the ninth and tenth reviews further mutants against the 87-case one; their survivors are the gaps listed above, apart from mutants each review showed no real log or supported platform can distinguish (for example, the order of the build-failure and named-failure checks, and writing mutants with `write_text` on macOS). Tooling mutations run against the standalone verifier, not XCTest.

**Real-Xcode checks:** the final runner's SHA-256 is `102b16c213f6532e030e737f70e18d351311a29370521fa4a7b3e26bc46dd092` and the final verifier's is `501df7dfc09ee4616afbb2db4ffa50e11245aaf426b943f010db00ab00d5776a`. The main agent ran that runner over this repository with `ClipboardTTSAppTests/MockURLProtocolTests` selected: the control read `SURVIVED`, unregistering the factory manager's session `KILLED` by `testFactoryManagerSessionIsRegisteredForTeardownToInvalidate`, an assertion-message change `SURVIVED`, and a syntax error `BUILD-FAILED`, with exit 0, and `results.json` recorded a complete run with exit 0, `HEAD` `7f1e628`, the control's verdict, and all three mutants. A mutant of `Sources/Info.plist` was refused after the passing control, naming that path and the mutant, with exit 2 and no verdict; its `results.json` recorded an incomplete run with exit 2, that error, the passing control, and no mutant. The third review's real runs read a one-test selection's `Executed 1 test` summary as a passing control, and a `project.yml` mutant as `SURVIVED` with the copy's `Info.plist` restored afterwards. The second review's real runs read `XCTFail`, a thrown error, an expectation timeout, and an unmet `XCTExpectFailure` as `KILLED`, a compile error and a SwiftLint violation as `BUILD-FAILED`, a skipped test beside a passing one and an expected failure as a passing control, and `fatalError` and `exit` inside a test as `FAILED-UNNAMED` (NB34).

**Application gates and provenance:** on 2026-09-25 the exact target state of the second review, whose build inputs equal `HEAD`'s because only Markdown and the two untracked scripts differ, passed `xcodegen generate`, `./check-coverage.sh`, and `xcodebuild -project ClipboardTTSApp.xcodeproj -scheme ClipboardTTSApp -configuration Debug SWIFT_STRICT_CONCURRENCY=complete CODE_SIGNING_ALLOWED=NO build` in an owned scratch copy on macOS 26.7 (25G229), arm64, Xcode 27.0 (27A266a), SwiftLint 0.65.1, XcodeGen 2.46.0, and Python 3.13.9; that run is also the `HEAD` baseline. The final candidate, after the second through tenth reviews' fixes and again after NB36, NB37, and the twelfth review's fix, passed the same gates again from cleared derived data with identical results each time. The coverage run executed 392 tests with zero failures; Managers measured 2207/2237 (98.65%) and application logic 3525/3604 (97.80%), with TextExtractionManager's 78.57% within the unchanged aggregate policy. Strict SwiftLint reported zero violations in 109 files. The only source warning is N7's at `AudioPlayerManager.swift:275`; the accepted tooling diagnostic is the skipped AppIntents metadata extraction. The previous agent reported the same gate results for `HEAD` and its candidate in the same environment with Python 3.9.6.

## Inherited implementation and review history

**Provenance and limits:** the following account was supplied by the previous agent in the uncommitted candidate based on HEAD `7f1e6289d1119590426db4ad206930d04face996`. At intake the runner SHA-256 was `af794393bda25ad0bc449ef8cf34f9fb30d00afa43c9217057535b54742b9038` and verifier SHA-256 was `35b2596cc330152c1be2bc10b9cdd62647938baa7fc1cc85e26c91fb5344e8c1`. No artifacts supporting its 72-mutant campaign were supplied, so that campaign and the earlier review counts remain historical, not independently validated current evidence. The inherited account identifies Xcode 27.0 for its skipped-test probe; other historical environment details and intermediate candidate revisions were not supplied. The current review and checks above supersede it as evidence for the current candidate.

**S2 — Done (user decision, 2026-09-24).** Mutation evidence had been produced by a new scratch script for each task — NB25, NB26, and NB30 each wrote one — and each relearned the same pitfalls: a verdict must come from a named failing test rather than an exit code or an `Executed N tests` line, a log with no terminal marker is unfinished, a mutant can fail to build, and an interrupted run left a mutant applied in its copy, which NB30's run did on 2026-09-24. `run-mutants.py` now holds those rules. It reads a JSON spec of exact-text mutants, validating its whole shape and refusing any key it does not document and any string no file or process argument can carry, a NUL or a lone surrogate that JSON can encode, and builds one scratch copy outside the repository from `git archive HEAD`, every path `git diff HEAD` names mirrored from the working tree (copied while it is still in the index, or removed otherwise: a path removed from the index is deleted in the target state even when a file, possibly ignored, remains on disk, and a path beneath a working-tree symlink is one Git reports as deleted and is never followed; removals are applied before copies, because a case-only rename names one file twice on a case-insensitive volume), and explicitly named untracked files, which must be regular files: a symlink would bring in whatever it points at. Changed paths are copied rather than patched in, because `git apply` run in the copy would discover any repository around `--out` and could skip every path it judged outside the copy, however a ceiling on that discovery was spelled. `--out` must lie outside the repository and must not contain it, since rebuilding the copy deletes what lies inside `--out`, both judged by file identity over existing ancestors because a case-insensitive volume lets a differently cased path name a directory inside another, and must be new, empty, or already marked as the runner's own, and a child it deletes or writes that is a symlink is refused first, so a mistyped or shared directory is never cleared and nothing is written through a link. The previous run's `results.json` is removed before any child is checked, and its logs before the spec is read, so a refused run leaves no evidence that looks like its own. Spec paths must be relative without `..` and must resolve inside the copy, so a symlink cannot lead an edit out of it, and mutant names, which become log file names, are limited to letters, digits, `.`, `_`, and `-`, and must be unique, and other than `control`, regardless of case, because macOS volumes usually ignore it. Nothing is generated before the edits are planned, so a file that exists only once generated cannot be an edit target; a tracked file that generation rewrites, such as `Sources/Info.plist`, is refused before testing instead, as the current review above describes. Edit targets are keyed by file identity rather than spelling, because a case-insensitive volume lets two spellings name one file. Before building, it replays each mutant's edits in order and refuses the whole run unless every edit's non-empty old text matches exactly once where it applies, counting overlapping matches, since `aa` occurs twice in `aaa`; the replayed texts are what the run writes. It refuses before any mutant if the unmutated control does not pass, or if any selector ran no test in it, a skipped test counting as none: the run's total count cannot tell, because one selector that matches makes it positive while a misspelled one beside it runs nothing. Each mutant's planned texts are written inside the `try` that restores them. SIGINT and SIGTERM are recorded rather than raised: the handler stops the running test tool, a tool started after a signal is stopped at once, and the runner stops only after the mutant's restore and regeneration, so no signal can land between a test run and its restore. After every mutant the copy's edited files are restored and its project regenerated, so a mutant of a generator input leaves no generated state behind, and only then are they hashed against their pristine state. A pure `classify` decides on the log's last standalone terminal-marker line, because test output can contain the marker's text, and reads named failures and build errors only when that marker is a failure, because a passing run's output can contain failure-shaped text. It reads each log as `KILLED` with every test it names failing, `SURVIVED` only after `** TEST SUCCEEDED **` with the run's final summary counting at least one executed test that was not skipped, reading both the plain summary and Xcode 27's `Executed 3 tests, with 2 tests skipped and 0 failures`, whose executed count includes the skipped ones, `NO-TESTS` for a success that executed none, `BUILD-FAILED` with its first errors, `FAILED-UNNAMED` for a finished failure that names no test, or `NO-VERDICT` without a terminal marker. `XCTExpectFailure` prints `Expected failure in -[…]` rather than `error: -[…]`, which the passing gate log of 2026-09-24 showed, so an expected failure is not read as a kill. README documents the procedure, and AGENTS' mutation rule sends mutants of code under XCTest through it; by user decision, tooling scripts such as this runner and the coverage gate keep being mutated against their own verifiers, because the runner drives XCTest only and judging anything else by exit code is what it exists to avoid.

**Evidence** (2026-09-24). `verify-mutation-runner.py` passes 66 cases, and each reports its own crash as its failure rather than ending the verifier: nineteen feed `classify` the log shapes xcodebuild produces, including an over-fulfilment abort reporting `Executed 0 tests` after a named failure, a per-suite summary with no terminal marker, a success that executed no test, two named failures, success-marker text inside test output, failure-shaped or build-failure text inside a passing run, an expected failure inside a failed one, and Xcode 27's summaries with a skipped test beside one that ran and with every test skipped; nine check which started test cases each kind of selector matches, including a suite whose every test skipped; thirty-eight run the real script in a throwaway git repository whose stand-in `xcodebuild` records the copy's state and the selectors it was given and prints the log a real run would print, and whose stand-in `xcodegen` copies the project spec into a generated file. Seventy-two mutants of the runner were each run against the verifier, and each fails a case the verifier names; a mutant counts as killed only when it does: reverting the terminal-marker check, the control check, the executed-test count, the per-selector check or its matching of suites, parenthesized test names, and started rather than passed or skipped cases, reading the skipped-test summary, subtracting skipped tests from the executed count, the edit plan or its in-order replay, uniqueness or its counting of overlapping matches, non-empty old text, path confinement, the relative-path check, the name restriction or its case-insensitive uniqueness or reservation of `control`, planning edits before any generation, keying edit targets by file identity, the spec-shape checks, unknown-key refusal at each of its three levels, refusing a NUL or a lone surrogate or checking selectors, untracked paths, or edit text for them, the out-of-repository check, the ownership check, the symlinked-child refusal, the untracked-file check, its symlink refusal, its NUL-separated listing, the tracked diff or its mirroring of deletions, copying only paths still in the index, applying removals before copies, treating any path beneath a working-tree symlink as deleted rather than checking only its immediate directory, containment by file identity in either direction or its handling of an `--out` not yet created, the removal of stale logs or of stale results or clearing results before the child checks, the restore, the regeneration after it, recording signals rather than raising them, stopping a tool started after a signal, either signal handler, `--only` or its unknown-name refusal, or passing every selector; deciding on marker text anywhere or on any success marker rather than the last standalone one, reading failures before that marker, reading `with 0 failures` as survival, reading a build failure as survival, deciding on an earlier suite's summary, reporting only the first named failure, exiting 0 with a mutant unjudged or unexercised, and letting an undecodable spec or a test tool that cannot start raise; and, as an over-restriction, counting an expected failure as a kill. The SIGINT handler is not redundant with Python's default: a process a non-interactive shell starts in the background inherits SIGINT as ignored, Python then installs no handler, and the verifier's SIGINT case launches the runner that way, which is what fails when the handler is removed. A real run over this repository, focused on `MockURLProtocolTests`, reported the control as `SURVIVED`, removing a factory manager's session registration as `KILLED` by `testFactoryManagerSessionIsRegisteredForTeardownToInvalidate`, NB30's known equivalent survivor as `SURVIVED`, and a syntax error as `BUILD-FAILED`, and left the copy matching the candidate. A real run whose spec selected a misspelled suite showed that `xcodebuild` then prints `Executed 0 tests` and `** TEST SUCCEEDED **`; the runner read that control as `NO-TESTS` and refused the run.

**Review.** The first adversarial review found three Blocking defects, each validated against the code before it was fixed, and one Non-blocking one, which the user chose to fix in the same change. Spec paths were joined to the copy unchecked, so `../` or an absolute path edited a file outside it, and mutant names could place logs outside `--out`. `SURVIVED` required only `** TEST SUCCEEDED **`, so a selector that matched nothing let every mutant survive. Edits were checked one at a time against the pristine file but applied in sequence, so two edits could interact and a repeated edit silently did nothing. The Non-blocking finding was that a spec that is not an object, or carries mistyped fields, raised a traceback with exit 1, the code reserved for an unjudged mutant. The verifier cases added for them fail on the first candidate, which passed 17 of 23 before the fixes.

A second review found two more Blocking defects and one Non-blocking one. A named untracked symlink was copied as its target's contents, so ignored or outside content could enter the copy; named untracked files must now be regular files. Four behaviors had no verifier case, so removing the SIGINT handler, reporting only the first named failure, accepting an unknown `--only` name, or passing only the first test selector each still passed; each now has a case and a mutant, and the review's point that AGENTS routed all mutation evidence through a runner that drives XCTest only was resolved by scoping that rule, by user decision. The Non-blocking finding, fixed by user decision, was that `git ls-files` output was split on whitespace, which refused an untracked path containing a space; it is read NUL-separated now. On the second candidate the combined untracked-file case fails at its spaced path, before it reaches the symlink, so that candidate passed 27 of 28; the symlink refusal is held by its mutant, which fails the same case.

A third review found one more Blocking defect and three Non-blocking ones, all fixed, the last three by user decision. With `--out` inside an unrelated Git worktree, `git apply` found that repository and skipped the tracked diff, so a run certified `HEAD` instead of the dirty target and still exited 0. The Non-blocking findings were that an unknown key such as `test` was ignored and the run fell back to the whole bundle, that a refused run left an earlier run's `results.json` in place, and that a mutant of `project.yml` left the generated project in its mutated state after the restore. The four cases added for them fail on the third candidate, which passed 28 of 32.

A fourth review found one more Blocking defect and one Non-blocking one, fixed by user decision. `--out` was checked only for lying outside the repository, after which the runner deleted `DIR/copy` and `DIR/logs` whatever they held and wrote logs through a symlinked `DIR/logs`, so a mistyped or shared directory could lose unrelated files or send writes elsewhere. The Non-blocking finding was that the text `** TEST SUCCEEDED **` anywhere in a log counted as success, so test output carrying it, followed by an unnamed crash and a final `** TEST FAILED **`, read as `SURVIVED`. The four cases added for them fail on the fourth candidate, which passed 32 of 36.

A fifth review found three more Blocking defects. A signal arriving during the post-restore regeneration cut it short and left the generated project mutated while the runner reported the copy restored. Named failures and build errors were read before the final marker, so a passing run whose output contained a failure-shaped line read as `KILLED`, which a real XCTest run reproduced. And this record's claim that every runner mutant failed a named case was false: reverting the terminal-marker check made `classify` raise, which ended the verifier before it named any case, so that kill was unattributed. The three cases added for the first two fail on the fifth candidate, which passed 36 of 39, and the harness now counts a kill only when the verifier names the failing case. Reading failures only from a failed run made the expected-failure case unable to tell a loosened failure pattern apart, so a case now holds that an expected failure inside a failed run is not reported as a killing test.

A sixth review found two more Blocking defects. Mutant names were unique and `control` was reserved only case-sensitively, so on a case-insensitive volume `CONTROL` overwrote the control's log and `case` and `CASE` overwrote each other's. And the copy was generated before edits were planned, so a generated file was an accepted edit target whose edit every run's regeneration then undid, giving a verdict for a mutant other than the one specified. The cases added for them fail on the sixth candidate, which passed 39 of 41.

A seventh review found one more Blocking defect and one Non-blocking one, fixed by user decision. Edit targets were keyed by spelling, so `Sources/Target.swift` and `sources/target.swift`, one file on a case-insensitive volume, were planned independently from its original text, which broke the in-order replay and the restore. The Non-blocking finding was that a test tool that could not be started once the control began raised a traceback with exit 1 rather than a diagnostic with exit 2. The two cases added for them fail on the seventh candidate, which passed 41 of 43; the alias case runs only where the volume ignores case, as this machine's does. **Limitation:** on a case-sensitive volume the alias cases return without asserting, so the verifier there holds neither identity keying nor case-aliased containment.

An eighth review found three more Blocking defects and one Non-blocking one, fixed by user decision. Repository containment and the Git ceiling compared spellings, so a differently cased `--out` wrote inside the repository and a differently cased path inside an unrelated worktree defeated the ceiling; containment is now judged by file identity, and changed paths are copied instead of patched. A signal arriving as the cleanup began raised before signals were held and skipped the restore while reporting it done; the handler now records the signal and never raises. And uniqueness counted non-overlapping matches only. The Non-blocking finding was that a refusal over a symlinked child left the previous `results.json` in place. The cases added for them fail on the eighth candidate, which passed 43 of 47; the deletion case, which the copying needs, passes there too, and a case for a signal arriving before the test tool starts was added when its mutant survived the first run of this round.

A ninth review found one more Blocking defect: containment was checked only one way, so an `--out` the runner already owned could contain the repository, as when a repository was later placed inside its `copy`, and rebuilding the copy deleted that repository. `--out` must now not contain the repository either. The case added for it fails on the ninth candidate, whose runner deleted the nested fixture repository.

A tenth review found one more Blocking defect and one Nit. Tests executed were checked only by the run's total, so a spec pairing a valid selector with a misspelled one passed the control while part of the requested selection never ran; every selector must now match a test case the control started. The Nit, fixed, was that README said a symlinked child is refused before anything is deleted or written, although the previous `results.json` is removed first by design. The cases added for it fail on the tenth candidate, which passed 49 of 57, and a real run whose spec paired `MockURLProtocolTests` with a misspelled suite was refused after its control.

An eleventh review found one more Blocking defect and one Non-blocking one. A tracked directory replaced in the working tree by a symlink made Git report its files as deleted, but the copying followed the link and brought in the content it pointed at; a changed path beneath a working-tree symlink is now treated as deleted. The case added for it fails on the eleventh candidate, which passed 57 of 58. The Non-blocking finding, fixed by user decision, was that a summary counting skipped tests was not read, so a passing run containing any skipped test was refused as `NO-TESTS`. A probe on 2026-09-25 with Xcode 27.0, one suite holding a passing and a skipped test and one whose only test skipped, showed the format and that the executed count includes skipped tests, and the verifier's summaries are copied from it. A test that skipped now counts as run by neither the total nor a selector. The cases added for it fail on the candidate before this fix, which passed 60 of 63; the all-skipped verdict case passes there too, because the old pattern read no count at all.

A twelfth review found no Blocking defect and one Non-blocking one, fixed by user decision: a test selector containing a NUL crashed the runner before any test ran, and a lone surrogate in an edit's text crashed it after the control, each with a traceback and exit 1 rather than a refusal with exit 2. Every string the spec supplies is now checked as the spec is read. The case added for it fails on the twelfth candidate, which passed 63 of 64.

A thirteenth review found one more Blocking defect: a tracked file removed from the index but left on disk, possibly ignored, was still named by `git diff HEAD` and copied into the copy, so ignored content could enter it past the named-untracked-file rule. A changed path is now copied only while it is in the index. The case added for it fails on the thirteenth candidate, which passed 64 of 65.

A fourteenth review found one more Blocking defect: a staged case-only rename such as `foo.swift` to `Foo.swift` is listed new name first, so the copying wrote the new spelling and then removed the old one, which on a case-insensitive volume is the same file, and neither survived. Removals now go before copies. The case added for it stages a rename in each direction and fails on the fourteenth candidate, which passed 65 of 66; a first version of the case committed one rename before the run and passed there, which is why both are now staged together.

**Limitation:** an interrupted run kills the `xcodebuild` it started; whether a test host that `xcodebuild` had already launched always exits with it was not verified, so check for owned processes after interrupting a run.
